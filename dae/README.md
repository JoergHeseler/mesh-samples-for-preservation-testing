TODO: All DAE files were validated with the [DAE validation tool](https://github.com/JoergHeseler/dae-validator-for-archivematica). DAE files ending in `-valid` pass validation, while all other files fail.
